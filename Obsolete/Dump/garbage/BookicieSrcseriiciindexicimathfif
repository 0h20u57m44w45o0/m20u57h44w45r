/* 
 * Nte: te fllwing mp is unique nly if
 * k, j, i in [0, kMx), [0, jMx), [0, iMx)
 */
dt[kMx][jMx][iMx] mpped t dt[idxMx]
dt[k][j][i] mpped t dt[idx]
int kMx, jMx, iMx, k, j, i;
int idxMx, idx; /* use lng type if needed */
idxMx = kMx * jMx * iMx;
idx = (k * jMx + j) * iMx + i;
