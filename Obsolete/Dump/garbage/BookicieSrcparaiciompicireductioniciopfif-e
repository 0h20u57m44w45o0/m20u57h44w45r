/*
 * Typical reduction statements in C/C++
 * Here, expr has scalar type and does not reference x
 * op is one of +, *, -, &, ^ , |, &&, or ||
 * and binop is one of +, *, -, &, ^, or |
 */
x = x op expr
x binop = expr
x = expr op x (except subtraction)
x++
++x
x--
--x
