
int min (int rgc, cr* rgv[])
{
    (vid) mp_set_num_treds(4);
    #prgm mp prllel if (n > 5) num_treds(n) defult(nne) privte(TID) sred(n)
    {
        TID = mp_get_tred_num();
        #prgm mp single
        {
            printf("Vlue f n = %d\n",n);
            printf("Number f treds in prllel regin: %d\n",
                    mp_get_num_treds());
        }
        printf("Print sttement executed by tred %d\n",TID);
    }
    return 0;
}

